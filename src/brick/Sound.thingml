thing fragment SoundMsgs {
  message beep();
}

thing fragment SoundController includes SoundMsgs {

  provided port sound {
    receives beep
  }

  abstract function beep() @abstract "true"

  statechart behavior init INIT {
    state INIT {
      internal event sound?beep
      action beep()
    }
  }
}
