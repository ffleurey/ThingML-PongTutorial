import "../DefaultBreakoutGame.thingml"

thing BreakoutGameNodeJS includes DefaultBreakoutGame {
	function quit() do
		println "done!"
		`process.exit(0);`
	end
	
	function timestamp() : Int64 return `process.hrtime.bigint()/1000000n` as Int64
}
